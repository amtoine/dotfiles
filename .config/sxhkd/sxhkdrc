super + Escape
    pkill -USR1 -x sxhkd

# FIXME: use real bindings to set the brightness and the volume
# XF86MonBrightness{Down,Up}
#     notify-send "main ({-,+})"
# XF86MonBrightness{Down,Up} + shift
#     notify-send "second ({-,+})"
# XF86AudioMute
#     gmixer --toggle --channel Master --notify
# ctrl + XF86AudioMute
#     gmixer --bluetooth --notify
# XF86Audio{Lower,Raise}Volume
#     gmixer --{down,up} --channel Master --step 5 --notify

super + Return
    st -e nu --commands '$env.SHELL = $nu.current-exe; tmux new-session -A -s (random uuid)'
super + {_,shift,alt} + @space
    rofi -show {drun,run,window}

super + shift + q
    logout.nu --lock slock --launcher rofi --no-ansi
super + p
    passme.nu -l 10 -bw 5 -p "Select a password to copy"

super + z; t
    notify-date.nu
